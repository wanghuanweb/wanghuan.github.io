
**js实现jquery选择器**

```
function getEle(str){
    //去除str开始结束空格，并且根据空格划分数组
    var arr = str.replace(/^\s+|\s+$/g,'').split(/\s+/g);

    for(var i = 0,len = arr.length;i < len;i++){
        getStr(arr[i]);
    }
}

function getStr(str){
    var elements = [];
    for(var i = 0;i)
}
```
